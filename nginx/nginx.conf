upstream blog {
  server blog-app:3000;
}

upstream resume {
  server resume-app:3000;
}

upstream llm {
  server llm-app:3000;
}

# blog — blog.yourdomain.com
server {
  listen 80;
  server_name blog.yourdomain.com;

  location / {
    proxy_pass http://blog;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}

# resume — resume.yourdomain.com
server {
  listen 80;
  server_name resume.yourdomain.com;

  location / {
    proxy_pass http://resume;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}

# llm — llm.yourdomain.com
server {
  listen 80;
  server_name llm.yourdomain.com;

  location / {
    proxy_pass http://llm;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
