---
title: "이더리움 스마트 컨트랙트 기초"
description: "Solidity를 사용한 이더리움 스마트 컨트랙트 개발의 기초를 배워봅니다."
category: "blockchain"
tags: ["ethereum", "solidity", "smart-contract", "web3"]
createdAt: "2026-01-12"
updatedAt: "2026-01-12"
published: true
---

# 이더리움 스마트 컨트랙트 기초

스마트 컨트랙트는 블록체인에서 자동으로 실행되는 프로그램입니다.

## Solidity 기본 문법

### 컨트랙트 구조

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

contract SimpleStorage {
    uint256 private storedValue;

    event ValueChanged(uint256 newValue);

    function set(uint256 value) public {
        storedValue = value;
        emit ValueChanged(value);
    }

    function get() public view returns (uint256) {
        return storedValue;
    }
}
```

## 주요 개념

### 1. 상태 변수

블록체인에 영구적으로 저장되는 데이터입니다.

### 2. 함수 가시성

- `public`: 외부에서 호출 가능
- `private`: 컨트랙트 내부에서만 호출 가능
- `internal`: 상속받은 컨트랙트에서 호출 가능
- `external`: 외부에서만 호출 가능

### 3. 이벤트

트랜잭션 로그에 기록되어 프론트엔드에서 구독할 수 있습니다.

## 결론

스마트 컨트랙트 개발은 보안이 가장 중요합니다. 충분한 테스트와 감사를 거친 후 배포하세요.
